package com.franklinwireless.android.jexkids.viewmodel.auth

import androidx.databinding.BaseObservable
import androidx.databinding.Bindable

class LanguageObserver : BaseObservable() {
    private var lanIndex: Int = 0

    /*@Bindable
    fun getSelectedItemPosition(): Int {
        return lanIndex
    }

    fun setSelectedItemPosition(selectedItemPosition: Int) {
        lanIndex = selectedItemPosition
        notifyPropertyChanged(BR.lanIndex)
    }*/
}